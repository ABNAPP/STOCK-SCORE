# Apps Script Web App URL Template
# 
# PRIMÄR METOD: Google Apps Script API ger 5-10x snabbare prestanda än CSV-proxy
#
# INSTRUKTIONER:
# 1. Kopiera denna fil till .env.local i samma mapp
# 2. Filen .env.local kommer automatiskt ignoreras av Git (säkerhet)
# 3. Ersätt YOUR_SCRIPT_ID med din faktiska Apps Script Web App URL
# 4. För Vercel: Sätt environment variable i Vercel Dashboard istället
#
# URL-format: https://script.google.com/macros/s/SCRIPT_ID/exec
# Viktigt: URL måste sluta med /exec (inte /library/...)

VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/AKfycby519iyhursADbzQUTTODBsL90qs1zXdUxSqGe4ifI1ZX8DOzN707ZtQld0_v65EtHKRw/exec

# Delta Sync Configuration (Optional)
# Delta sync enables efficient incremental updates - only fetches changes since last update
# Default: enabled (true), poll interval: 15 minutes
VITE_DELTA_SYNC_ENABLED=true
VITE_DELTA_SYNC_POLL_MINUTES=15

# API Token (Optional but recommended for delta-sync)
# Set this to the same token configured in Apps Script Script Properties (API_TOKEN)
# If not set, API requests work but without authentication
VITE_APPS_SCRIPT_TOKEN=
